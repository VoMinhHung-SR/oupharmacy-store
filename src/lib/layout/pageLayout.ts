/** Standard page vertical padding — aligned with cart / checkout. */
export const PAGE_Y = 'py-3 sm:py-4'

/** Slightly roomier blocks (PDP rails, campaign sections). */
export const PAGE_Y_SECTION = 'py-4 sm:py-5'

/** Home / marketing section vertical padding. */
export const SECTION_Y = 'py-6 sm:py-8'

/** Default vertical stack between page blocks. */
export const PAGE_STACK = 'space-y-4'
